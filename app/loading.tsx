import { FullPageLoader } from '@/components/ui/FullPageLoader'

/** Shown on hard refresh and while the App Router segment is loading. */
export default function Loading() {
  return <FullPageLoader />
}
