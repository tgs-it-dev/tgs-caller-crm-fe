import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Set up your account — TGS Caller CRM',
}

export default function InviteLayout({ children }: { children: React.ReactNode }) {
  return children
}
