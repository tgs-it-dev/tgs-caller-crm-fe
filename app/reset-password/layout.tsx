import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Choose a new password — TGS Caller CRM',
}

export default function ResetPasswordLayout({ children }: { children: React.ReactNode }) {
  return children
}
