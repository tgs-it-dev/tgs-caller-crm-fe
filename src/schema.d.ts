// Vendored OpenAPI-generated types (moved into frontend package)
// This file is kept for frontend-only development and should be regenerated
// from the backend OpenAPI when needed.

export type paths = {
  '/health': {
    get: {
      responses: {
        200: { content: { 'application/json': { status: string } } }
      }
    }
  }
}
